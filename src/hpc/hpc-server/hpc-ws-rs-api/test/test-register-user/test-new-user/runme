#!/usr/bin/env bash
# makeme 
# @author: George Zaki 
# 
# Copyright Leidos Biomedical Research, Inc
# 
# Distributed under the OSI-approved BSD 3-Clause License.
# See http://ncip.github.com/HPC/LICENSE.txt for details.
#


# Script to run this test
# Usage: runme [no arguments]

#import common functions
source ../../utils/functions

#Run common script
../../utils/runtest


RESPONSE_HEADER=response-header
RESPONSE_MSG=response-message.json
SERVER=`cat ../../utils/server`

curl -H "Content-Type: application/json" -d @input.json.tmp -X PUT ${SERVER}/hpc-server/user  --config ../../utils/config  -H "Accept: application/json" -D $RESPONSE_HEADER -o $RESPONSE_MSG -s 2> curl-status 
#echo "curl -H \"Content-Type: application/json\" -d @input.json.tmp -X PUT ${SERVER}/hpc-server/user  --config ../../utils/config  -H \"Accept: application/json\" -D $RESPONSE_HEADER -o $RESPONSE_MSG -s 2> curl-status "

check_connection $RESPONSE_HEADER

RESPONSE_CODE=`get_http_code $RESPONSE_HEADER`

echo $RESPONSE_CODE 1>&2
