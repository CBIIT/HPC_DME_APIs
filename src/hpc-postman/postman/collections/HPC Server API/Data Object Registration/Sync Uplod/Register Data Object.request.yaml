$kind: http-request
method: PUT
url: "{{baseUrl}}{{basePath}}/v2/dataObject/{{path}}"
order: 1787344951408
headers:
  Accept: application/json
pathVariables:
  path: "{{path}}"
scripts:
  - type: beforeRequest
    code: |-
      const now = new Date();
      const mm = String(now.getMonth() + 1).padStart(2, '0');
      const dd = String(now.getDate()).padStart(2, '0');
      const yyyy = now.getFullYear();
      const hh = String(now.getHours()).padStart(2, '0');
      const min = String(now.getMinutes()).padStart(2, '0');
      const ss = String(now.getSeconds()).padStart(2, '0');
      const timestamp = `${mm}-${dd}-${yyyy}-${hh}-${min}-${ss}`;
      const dynamicPath = `FNL_SF_Archive/eran-pi-lab/eran-project/eran-flowcell/eran-sample/eran-data-object-sync-${timestamp}`;
      pm.variables.set('path', dynamicPath);
      pm.collectionVariables.set('lastSyncUploadPath', dynamicPath);
    language: text/javascript
body:
  type: formdata
  content:
    - type: file
      key: dataObjectRegistration
      src: /Users/rosenbergea/Development/HPC-DM/hpc-dm-repo/src/hpc-postman/files/registration-sync-upload.json
      contentType: application/json
    - type: file
      key: dataObject
      src: /Users/rosenbergea/Development/HPC-DM/hpc-dm-repo/src/hpc-postman/files/data-object-binary.png
